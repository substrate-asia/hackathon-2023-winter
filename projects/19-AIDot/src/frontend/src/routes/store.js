/*import {writable} from 'svelte/store';

export const username = writable(localStorage.username || "");
export const password = writable(localStorage.password || "");

username.subscribe((value) => localStorage.username = value);
password.subscribe((value) => localStorage.password = value);
*/